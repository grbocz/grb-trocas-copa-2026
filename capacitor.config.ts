import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grbocz.trocascopa2026',
  appName: 'Trocas Copa 2026',
  webDir: 'dist'
};

export default config;
